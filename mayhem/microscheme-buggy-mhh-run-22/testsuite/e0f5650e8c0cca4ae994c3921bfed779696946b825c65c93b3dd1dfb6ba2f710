;; ======================= Microscheme ==lin==================
;; LED blinkled-pin)
	(pause 1000)1 Ryan Suchocki, et al.
;; http://github.com/ryansuchocki/microoutput 13))

(define (loop)
	(toggle led-pin)
	(pause 1000))

(forever lOop)
